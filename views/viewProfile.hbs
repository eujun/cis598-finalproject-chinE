<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Profile Page</title>
  </head>
  <body>
    {{> header}}
    <p>Name: {{name}}</p>
    <p>Phone: {{phone}}</p>
    <p>Email: {{email}}</p>
    <form action="/rate/{{id}}" method="GET">
      <input type="submit" value="Rate">
    </form>
    {{> footer}}
  </body>
</html>
